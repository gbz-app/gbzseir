# Veritabanı sözleşmesi (DB contract)

Foundation DB agent çıktısı, 2026-09-10. Updated 2026-09-11 for migrations 20260913* (changes are folded into the lines below; the last sections list what is new).

HOSTING: Supabase project in ap-northeast-1 (Tokyo); Vercel functions run in hnd1 (Tokyo). Keep vercel.json regions as they are.

GENERAL: import type { Database } from "@/lib/database.types" (generated: `scripts/db/gen-types.mjs` after every schema change). All ids uuid strings; timestamps ISO timestamptz; phones E.164 "+905XXXXXXXXX". NEVER select geography columns (neighbourhoods.center, businesses.location, poi.location: hex EWKB) – use generated lat/lng. RPC errors: PostgREST error.message = Turkish user-facing text, error.hint = machine code. Search normalisation public.tr_norm() == src/core/tr.ts trNormalize(). Embeds verified: listings?select=*,owner:public_profiles(display_name,created_at),business:businesses(name,phone,slug,logo_url,verification_level),listing_categories(name,slug),neighbourhoods(name),listing_media(url,thumb_url,sort); reviews?select=*,author:public_profiles!reviews_author_id_fkey(display_name); businesses?select=*,business_service_categories(service_categories(id,name,slug)),business_service_areas(neighbourhoods(id,name)),business_photos(*),reviews(count).

BANS: profiles.status 'banned' is enforced in the DB (private.is_banned): the user's businesses (with menus, photos, rooms, services, events, reviews), listings and own reviews are hidden from everyone but the user and admins (nothing deleted); ratings leave their reviews out; accept_lead, reply_review, submit_review, submit_report, submit_contact_message and events insert refuse a banned caller; dispatch skips their firms.

TABLES (RLS enabled everywhere):
- neighbourhoods(id, name [short, e.g. 'Hacıhalil'; UI may append ' Mah.'], slug uq, district 'Gebze', center, lat, lng, osm_id, created_at). 40 rows. Public read.
- app_settings(key pk, value jsonb, updated_at). Public read, admin write (audited). Keys: otp_demo_mode (THE single OTP switch, see supabase/README.md), feature_business_applications (enforced in apply_business, admins exempt), maintenance_banner, support_phone, support_email, listing_days 30, first_listings_moderated 3, listing_daily_cap 10 / listing_active_cap 50 (0 = no cap), max_providers_default 5 (1-10, used by categories without their own limit), request_redispatch_hours 6 (1-72), duty_data_mode 'demo'|'off'|'live', analytics_retention_days 180, audit_retention_days 730.
- profiles(id=auth uid, full_name<=80, phone uq E.164, avatar_url, email, neighbourhood_id, role 'user'|'admin', status 'active'|'restricted'|'banned', trusted_publisher, marketing_consent, marketing_consent_at [stamped by trigger on every change], kvkk_accepted_at, kvkk_version [stamped by trigger with the live KVKK legal_texts version], onboarded, is_demo, created_at, updated_at). Row auto-created on signup (phone filled). Own row select/update (admin all). A trigger silently keeps id, phone, role, status, trusted_publisher, is_demo, created_at for non-admins. Profile completion = update {full_name, neighbourhood_id, kvkk_accepted_at, marketing_consent, onboarded:true}. No insert/delete.
- VIEW public_profiles(id, display_name 'Ayşe Y.', created_at): anon+auth read. Only users with public content (an active/sold/filled listing, an approved business or a review), plus the caller's own row; admins see all. No phone, e-mail, avatar or neighbourhood.
- businesses(id, owner_id, slug uq auto, name 2-80, logo_url, cover_url, description<=2000, phone E.164 PUBLIC, address, location, lat, lng, neighbourhood_id, kinds text[] subset {service,shop,employer}, category_label, working_hours jsonb {"mon":{"open":"09:00","close":"18:00"},...,"sat":{...},"sun":null} [keys mon,tue,wed,thu,fri,sat,sun; null/missing = closed], status 'pending'|'approved'|'rejected'|'suspended', rejection_reason, verification_level 0-3 (>=1 'Onaylı'), vacation_mode, rating_avg numeric(3,2), rating_count, leads_accepted_count, search_norm, is_demo, created_at, updated_at, approved_at). Anon/auth read approved (owner not banned); owner reads and updates own (any status); admin all. Created only through apply_business. Trigger (non-admin): update keeps status, verification_level, rating_*, leads_accepted_count, approved_at, rejection_reason, slug, owner_id, is_demo. Editing a rejected business re-submits it as pending. Use kinds.includes('service'|'shop'|'employer'); there are no is_* booleans.
- business_service_categories(business_id, category_id) pk; business_service_areas(business_id, neighbourhood_id) pk; business_photos(id, business_id, url, sort, created_at): read if business public or own. Replace them atomically with set_business_service_scope / set_business_photos (max 20 photos).
- business_documents(id, business_id, path ['<uid>/...' in private-docs], kind 'vergi_levhasi'|'kimlik'|'meslek_belgesi'|'diger', created_at): owner/admin only.
- reviews(id, business_id, request_id?, author_id?, rating 1-5, comment<=1000, reply<=1000, replied_at, is_demo, created_at; uq(request_id,business_id)). Public read (banned authors hidden). Writes only via RPC (admin direct).
- service_categories(id, parent_id [null = top level], name, slug uq, icon [lucide], description, synonyms text[], sort, popular, active, max_providers [1-10 or null = app_settings.max_providers_default], notify_pool_size 8, auto_dispatch, search_norm, created_at). Public read; admin writes through admin_save_service_category / admin_delete_service_category (refuses while sub-categories, firms or requests use it) plus direct updates of the flags.
- question_flows(id, category_id, version, schema jsonb {steps:[{id,type:'single'|'multi'|'number'|'text'|'date',title,help?,required?,options?[{value,label}],min?,max?,unit?,placeholder?,showIf?{step,in[]}}]} [same as src/core/flow.ts; system steps NOT included], published, created_at; uq(category_id,version)). Public reads published: .eq('category_id',id).eq('published',true).order('version',{ascending:false}).limit(1).
- service_requests(id, public_code 8 chars [A-Z2-9] uq, customer_id, category_id, flow_id, answers jsonb, neighbourhood_id, address_note<=200, when_type 'acil'|'bu_hafta'|'tarih'|'esnek', when_date, note<=1000, photos text[]<=6 [private-docs paths '<uid>/requests/...', served by /api/talep-foto], hide_phone, status 'admin_review'|'open'|'filled'|'closed_hired'|'closed_cancelled'|'expired'|'no_match', accepted_count, max_providers, hired_business_id, dispatch_note ['area_fallback'], stalled_at [a next wave found no new firm; cleared by a dispatch that creates leads; UI shows 'Firma aranıyor'], is_demo, created_at, updated_at, closed_at). Customer reads own ("Taleplerim": select('id,public_code,status,accepted_count,max_providers,stalled_at,created_at,service_categories(name,icon),neighbourhoods(name)').eq('customer_id',uid)); admin read/update; writes via RPC. Open requests without an accepted firm get a next wave after request_redispatch_hours (daytime only); requests expire after 14 days.
- leads(id, request_id, business_id, status 'sent'|'seen'|'accepted'|'declined'|'closed_full'|'removed_by_customer', offer_price_try, offer_note<=280, wave_no, match_score, seen_at, accepted_at, created_at, updated_at; uq(request_id,business_id)). Readable by the business owner and the request's customer; admin all; writes via RPC.
- VIEW my_leads (authenticated; the caller's business only, admin all): id, request_id, business_id, status, offer_price_try, offer_note, wave_no, seen_at, accepted_at, created_at, request_status, when_type, when_date, accepted_count, max_providers, request_created_at, category_id, category_name, category_slug, category_icon, neighbourhood_id, neighbourhood_name, photo_count, has_note. No customer identity.
- poi(id, kind 'pharmacy'|'mosque'|'bus_stop'|'place'|'taxi'|'atm', name, slug uq, address, phone, location, lat, lng, neighbourhood_id, details jsonb, source 'kbb'|'osm'|'manual'|'demo', source_ref, license, hidden [left out of every public read, RPC and the sitemap], locked [admin-edited fields survive sync upserts], last_seen_at, missing_since [set when a sync hid the row], search_norm, created_at, updated_at; uq(source,source_ref)). details for place: {category:'tarihi'|'park'|'doga'|'muze'|'avm'|'diger', description?, curated:bool, photos:[], hours?, fee?, wikidata?}; for bus_stop: {lines:string[] (often empty), stop_code?, shelter?}. Show curated places first. Public read, admin write (/admin/yerler edits every kind).
- pharmacy_duty(id, poi_id, duty_start, duty_end, source 'demo'|'manual'|importer id e.g. 'nosyapi', note, fetched_at; uq(poi_id,duty_start); window 08:30 -> 08:30 Istanbul). Public read. duty_data_mode: 'demo' = the random roll_demo_duty list (app labels it "Örnek veri"); 'off' = no list (app shows the Eczacı Odası link), roll stops; 'live' = every source except 'demo', roll stops.
- duty_import_runs: log of imports and manual saves (admin read, /admin/nobet).
- news_sources(id, name, site_url, feed_url uq, active, last_fetched_at, last_error, fail_count, failing_since, permission_note [NOT readable through the API; admin_news_sources()], created_at). Public read of the other columns.
- news_items(id, source_id, guid, title, summary<=280, url, published_at, title_hash, category, created_at; uq(source_id,guid)): written by /api/cron/news (service role, onConflict 'source_id,guid'; news_record_fetch stores each feed's status). Public read.
- announcements(id, kind 'su_kesintisi'|'elektrik_kesintisi'|'belediye'|'genel', title, body, neighbourhood_ids uuid[], source_label, starts_at, ends_at, created_by, is_demo, created_at). Public reads rows with ends_at null or > now(); admin CRUD.
- listing_categories(id, type 'classified'|'job', parent_id, name, slug uq [job sectors 'is-*'], icon, sort, is_banned, attributes_schema jsonb [{key,label,type:'text'|'number'|'select'|'boolean',options?[{value,label}],required?,filterable?}], created_at). filterable (select, number, boolean) = an /ilanlar filter (search_listings p_attrs). Pickers: .eq('is_banned',false). Public read.
- listings(id, type, owner_id, business_id, category_id, title 3-100, description<=4000, price_try, attributes jsonb, neighbourhood_id, status 'draft'|'pending_review'|'active'|'rejected'|'expired'|'sold'|'filled'|'paused'|'deleted', rejection_reason, flags text[] ['iban','odeme','telefon','url','emlak_vasita','ayrimcilik'], expires_at, published_at, deleted_at [set by trigger; the row and its photos are purged 30 days later], view_count, call_count, job_work_type 'tam_zamanli'|'yari_zamanli'|'vardiyali'|'stajyer'|'gunluk', job_salary_min, job_salary_max, job_salary_hidden, job_experience 'farketmez'|'0-1'|'1-3'|'3+', job_benefits text[] ['servis','yemek','sgk','prim'], job_location_label, search_norm, search_tsv, is_demo, created_at, updated_at).
  - Read: public reads status in (active, sold, filled) with an unbanned owner; owner reads all own; admin all.
  - Insert (authenticated): send {type, category_id, title, description, price_try, attributes, neighbourhood_id, job_* fields, status?:'draft'}. The trigger sets owner_id and flags, then status: pending_review for the first 3 or when flagged; active if trusted_publisher or >=3 published. It also sets published_at/expires_at (+30 days). Caps: listing_daily_cap new per 24 h, listing_active_cap open listings. Jobs need an approved business (business_id is set automatically), else 403 hint business_required; banned category -> 400 hint banned_category.
  - Owner status transitions: active->paused; paused->active (if not expired); active|paused|expired->sold (classified) or filled (job); any->deleted; draft|rejected->pending_review (resubmit) or draft. Anything else -> 403 hint invalid_status_transition. Owner cannot change expires_at, published_at, counters, business_id or type (use renew_listing).
  - Owner hard-delete only when status is draft, rejected or pending_review.
- listing_media(id, listing_id, url, thumb_url, sort, created_at): public read if the listing is public. Replace with set_listing_media (max 10).
- favorites(user_id, target_type 'listing'|'business'|'poi', target_id, created_at; pk all three): own CRUD. A hard-deleted listing takes its favorites with it.
- reports(id, reporter_id?, target_type 'listing'|'business'|'review'|'user', target_id, reason 'dolandiricilik'|'yanlis_kategori'|'uygunsuz'|'yaniltici'|'diger', detail<=1000, status 'open'|'resolved'|'dismissed', resolved_at, created_at): insert only through submit_report; reporters read their own rows; admin read/update. Admin notes live in report_notes (admin only).
- contact_messages: insert only through submit_contact_message (topic 'bilgi_duzeltme' = "Bilgi hatalı mı? Bildir"; guests may send it without phone / e-mail; guests throttled per IP hash 5/h and 60/h overall); admin read/update.
- contact_events(id, user_id, subject_type 'listing'|'job'|'business'|'poi'|'lead', subject_id, event 'phone_reveal'|'call_click'|'directions', ip_hash, created_at): admin read; writes via RPC only.
- notifications(id, user_id, type, title, body, link, read_at, push_sent_at, push_attempts, push_error, push_attempted_at, created_at): own read/delete; clients may update read_at only (mark_notifications_read). Insert unsent (push_sent_at null) and the push path delivers it; push_sent_at set at insert = in-app only (night notices).
  - lead_new (link /isletme/talepler/<leadId>), lead_reopened, lead_accepted (/talep/<code>), request_created, request_expired (/talep/<code>), request_hired (/isletme/talepler/<leadId>), review_new (/isletme/yorumlar)
  - listing_approved (/ilan/<id> or /is-ilani/<id>), listing_rejected (/profil/ilanlarim or /profil/is-ilanlarim)
  - business_approved (/isletme), business_rejected (/isletme/basvuru)
  - admin only (links under /admin are never pushed): business_application, request_review, request_no_match, request_stalled, news source failing, duty list missing.
- push_subscriptions(id, user_id, endpoint uq, p256dh, auth, user_agent, created_at): own CRUD.
- legal_texts(id, slug 'kvkk'|'acik-riza'|'kosullar'|'gizlilik'|'cerez', version, title, body_md, pending_review, published_at, created_at, updated_at): public reads published versions whose published_at has passed (live = newest per slug); published rows are immutable; admin writes (publishing is stamped with the server clock).
- vertical_subcategories (Keşfet chips per vertical: key, label, keywords[], exclude[], sort, active), amenities (scope 'business'|'room', key, label, icon, verticals[], sort, active), event_categories (key, label, icon, sort, active; events.category is a foreign key to key): public read, admin write, keys immutable, used rows cannot be deleted.
- audit_log: admin reads through admin_audit_log / admin_user_overview; admin changes are audited with actor_id. Rows older than audit_retention_days are purged nightly.
- data_sync_runs: one row per POI sync run or admin request (/admin/veri). finance_entries.document_path: receipt in private-docs/finance/<entry id>/ (admin only).
- demo_otp: no API access.

STORAGE:
- 'media': public URLs; jpeg/png/webp/gif; <=5MB; write/delete only under `${uid}/...` (e.g. `${uid}/listings/<listingId>/1.webp`); use getPublicUrl. Listing (select) only for the owner's folder or admins (anon cannot list).
- 'private-docs': private; jpeg/png/webp/pdf; <=10MB; `${uid}/...` (business documents, `${uid}/requests/` request photos); owner+admin read via createSignedUrl. `finance/` is admin only.

RPC (supabase.rpc(name,args)) — PUBLIC (anon+auth):
- nearby_pois({p_kind?:'pharmacy'|'mosque'|'bus_stop'|'place'|'taxi'|'atm'|null, p_lat?, p_lng?, p_radius_m?=5000 (max 50000), p_limit?=50 (max 500)}) -> [{id,kind,name,slug,address,phone,lat,lng,neighbourhood_id,neighbourhood_name,details,source,license,updated_at,distance_m|null}]. Nearest first; by name when no coordinates. Hidden rows left out.
- duty_pharmacies_now({p_lat?,p_lng?}) -> [{duty_id,poi_id,name,slug,address,phone,lat,lng,neighbourhood_id,neighbourhood_name,duty_start,duty_end,source,note,fetched_at,distance_m|null}]. Only windows containing now(); filtered by duty_data_mode.
- duty_pharmacies_for_day({p_date:'YYYY-MM-DD',p_lat?,p_lng?}) -> same shape. Windows overlapping [date 08:30, date+1 08:30) Istanbul time and not yet ended (use for "Yarın").
- neighbourhood_for_point({p_lat,p_lng}) -> [{id,name,slug,district,method:'polygon'|'nearest'}] (0-1 rows; send rounded coordinates).
- get_demo_otp({p_phone}) -> string|null. Null when otp_demo_mode is off, the phone is an admin, or no code in the last 5 min.
- log_contact_event({p_subject_type,p_subject_id,p_event}) -> void. Call before navigating to tel:; call_click on listing/job increments call_count (throttled per IP hash).
- reveal_listing_phone({p_listing_id}) -> {ok:true,phone,display_name} | {ok:false,reason:'not_found'|'rate_limited'|'login_required'|'no_phone'}. Business phone for business listings. Limits for 2. el: members 30/24h, guests 5/day per IP hash (guests over the limit get 'login_required'); jobs unlimited. CALL FROM THE BROWSER (the IP hash comes from the edge headers).
- increment_listing_view({p_listing_id}) -> void (throttled per IP hash).
- search_listings({p_type?='classified'|'job'|null, p_q?, p_category_id? (includes children), p_neighbourhood_id?, p_min_price?, p_max_price?, p_work_type?, p_sort?='newest'|'price_asc'|'price_desc', p_attrs?: jsonb}) -> SETOF listings (active and not expired). p_attrs = the /ilanlar `a_<key>` URL params without the prefix, e.g. {"marka":"apple","ekran_boyutu_min":"40","takas":"1"}; only the filterable fields of p_category_id (or its parent) are read: select = equality, boolean = "1", number = <key>_min / <key>_max. Supports .select('id,title,...,listing_media(url,thumb_url),neighbourhoods(name)') and .range(); select explicit columns, not the search_* columns.
- global_search({p_q,p_limit?=5 (max 20)}) -> {listings:[...], businesses:[...], services:[...], pois:[...], events:[...], articles:[...]}. Empty arrays when the query is under 2 chars; Turkish-insensitive (İ/ı). events: published, not ended, organizer public; articles: published news_articles.
- service_provider_counts() -> approved firms per service category.
- submit_contact_message({p_topic,p_message,p_subject?,p_name?,p_phone?,p_email?,p_business_name?,p_page_path?,p_user_agent?}).
- Helpers: tr_norm(t), tr_slug(t), short_name(p_full_name), tr_match(p_norm,p_q); is_admin(), owns_business(p_business_id), owns_listing(p_listing_id), business_is_public(p_business_id) -> boolean.

RPC — AUTHENTICATED:
- renew_listing({p_listing_id}) -> {ok:true,status,expires_at} | {ok:false,reason:'not_found'|'invalid_status'}.
- set_listing_media({p_listing_id,p_media}), set_business_photos({p_business_id,p_photos}), set_business_service_scope(...): delete + insert in one transaction (owner or admin).
- submit_report({p_target_type,p_target_id,p_reason,p_detail?}): login required, one open report per user and target, 20 per day.
- submit_service_request({p_category_id (sub-category), p_answers, p_neighbourhood_id, p_address_note?, p_when_type?='esnek', p_when_date?, p_note?, p_photos?=[] (private-docs paths), p_hide_phone?=false}) -> {id, public_code, status:'open'|'admin_review'|'no_match', lead_count}.
  - Validates answers against the latest published flow and keeps only visible-step answers.
  - Error hints: login_required, account_banned, category_not_found, neighbourhood_required, invalid_when ('tarih' needs a date today..+180), invalid_answers (message lists step ids), rate_limited (5 per 24h).
  - Auto categories dispatch immediately; concierge categories notify admins.
- get_request_for_customer({p_code}) -> null (not found or not yours) | {
  request: {id, public_code, status, category:{id,name,slug,icon,parent_name,parent_slug}, neighbourhood:{id,name,district}|null, address_note, when_type, when_date, note, photos[], hide_phone, answers:[{id,title,type,value,display}], accepted_count, max_providers, sent_count, stalled_at, hired_business_id, created_at, closed_at},
  providers: [{lead_id, status:'accepted', offer_price_try, offer_note, accepted_at, business:{id,name,slug,logo_url,rating_avg,rating_count,verification_level,phone,category_label}}],
  review: {id,rating,comment,reply,created_at}|null }.
- customer_remove_lead({p_lead_id}) -> {ok:true} | {ok:false,reason:'not_found'|'invalid_status'}. Frees the slot; a filled request re-opens, closed_full leads return to 'seen' and their firms get 'lead_reopened'.
- close_request({p_code,p_hired_business_id?}) -> {ok:true,status:'closed_hired'|'closed_cancelled'} | {ok:false,reason:'not_found'|'invalid_status'|'not_accepted'}.
- submit_review({p_request_code,p_business_id,p_rating 1-5,p_comment?}) -> {ok:true,review_id} | {ok:false,reason:'not_found'|'not_hired'|'invalid_rating'}. Hired firm only; re-submitting updates; rating_avg/count recomputed.
- get_lead_detail({p_lead_id}) -> null (not your business) | {
  lead: {id,business_id,status,offer_price_try,offer_note,wave_no,seen_at,accepted_at,created_at},
  request: {id,status,category:{id,name,slug,icon,parent_name}, neighbourhood:{id,name,district,lat,lng}|null, when_type,when_date,note,photos[],answers[],accepted_count,max_providers,address_note (null until accepted),created_at},
  customer: {display_name ('Ayşe Y.' before accept, full name after), phone (only when accepted and not hide_phone), hide_phone},
  can_accept: boolean }. Marks the lead sent->seen.
- accept_lead({p_lead_id,p_offer_price?,p_offer_note? <=280}) -> {ok:true, customer_name, customer_phone|null, hide_phone, accepted_count, max_providers, already?:true} | {ok:false, reason:'full'|'closed'|'removed'|'declined'|'business_not_approved'|'not_found'}.
  - Atomic: max max_providers firms; when full the request becomes 'filled' and remaining leads 'closed_full'. Notifies the customer; logs the phone reveal.
  - Error hints: note_too_long, invalid_price.
- decline_lead({p_lead_id}) -> {ok:boolean}.
- reply_review({p_review_id,p_reply}) -> {ok:boolean}.
- apply_business({p_name, p_kinds, p_phone? (defaults to the profile phone), p_category_label?, p_description?, p_address?, p_neighbourhood_id?, p_service_category_ids?=[], p_service_area_ids?=[] (defaults to [p_neighbourhood_id]), p_working_hours?={}, p_lat?, p_lng?, p_logo_url?, p_cover_url?}) -> {ok:true,business_id,slug,status:'pending'} | {ok:false,reason:'invalid_kinds'|'invalid_phone'|'categories_required'|'already_exists',business_id?,status?}.
  - Error hints: login_required, invalid_name. Raises when feature_business_applications is off (admins exempt).
  - Re-submits a pending or rejected application; notifies admins.
- mark_notifications_read({p_ids?}) -> number of rows updated (all unread when p_ids is omitted).
- delete_my_account() -> {ok:true}. Call only through the server action deleteMyAccount (src/features/profile/actions/delete-account.ts), after the user re-verified the SMS code: the RPC runs first with the user's session (also removes their events; audit details keep only masked phones), then the action removes every file under `<uid>/` in media and private-docs with the service role and expires the public pages; the client signs out.

RPC — ADMIN (signed-in admins only; anon has no execute, others get 42501 'Yetkisiz'):
- admin_review_business({p_business_id,p_approve,p_reason?}) -> {ok,status} | {ok:false,reason:'not_found'}. Approve sets verification>=1 and notifies 'İşletmen yayında!'.
- admin_review_listing({p_listing_id,p_approve,p_reason?}) -> {ok,status}. Approve sets active, published_at and +30 days, sets trusted_publisher at >=3 published, and notifies the owner.
- dispatch_request({p_request_id,p_wave?=1,p_business_ids?}) -> {ok:true,lead_count,total_leads,fallback,status} | {ok:false,reason:'not_found'|'closed',status?}. This is "Eşleştir ve gönder"; p_business_ids hand-picks firms. admin_request_candidates({p_request_id}) lists the candidates.
- admin_set_user_status({p_user_id,p_status,p_reason?}): profiles.status + Auth sign-in ban + (on a ban) the user's sessions, in one transaction.
- admin_save_service_category(...) / admin_delete_service_category({p_id}); admin_publish_flow({p_category_id,p_schema}).
- admin_set_duty({p_day,p_poi_ids}): the manual duty list of a day (source 'manual'; importers never overwrite it).
- admin_clear_demo_data({p_scopes}): scopes include events, finance, news_articles, demo_admin (only while a real admin exists); 'businesses' also removes demo events and finance rows; 'duty' sets duty_data_mode 'demo' -> 'off' and unschedules the demo roll. The caller removes media/demo/ photos. admin_data_health() reports demo counts and the demo duty job.
- admin_poi_sync_now({p_dry_run}): queues the public app's /api/cron/poi-sync; the admin page polls data_sync_runs.
- admin_news_sources() (with permission_note), admin_refresh_news_now() (queues /api/cron/news).
- admin_audit_log({p_prefixes?,p_actor?,p_user?,p_entity_id?,p_q?,p_from?,p_to?,p_limit?,p_offset?}), admin_user_overview, admin_dashboard, admin_analytics, admin_online_now, admin_finance_summary.
- Everything else the admin does through tables (is_admin RLS): category flags, reports, announcements, poi, legal_texts, vocabularies, settings.

SERVICE ROLE ONLY: claim_push_notifications({p_limit}), news_record_fetch(...), duty_import_record(...), poi_sync_apply(...). Auth hook: send_sms_hook(event) (accepts only +905XXXXXXXXX; stores the code in demo_otp).

CRON (pg_cron, UTC; the pg_net jobs POST to the public app with Vault 'gebzem_push_webhook_secret' = CRON_SECRET):
- gebzem-expire-listings 00:05 (expire_listings); gebzem-roll-demo-duty 05:31 (no-op unless duty_data_mode = 'demo'); gebzem-analytics-retention 03:17; gebzem-audit-retention 03:23.
- gebzem-purge-listings 03:41 -> /api/cron/purge-listings; gebzem-push-retry every 10 min -> /api/notifications/push (only when a claimable row exists).
- gebzem-request-redispatch at :13 and :43 (pure SQL: expiry, then next waves outside 22:00-08:00 Istanbul).
- gebzem-refresh-news every 20 min -> /api/cron/news; gebzem-duty-import 05:35 and gebzem-duty-import-recheck 06:10, 09:10, 15:10 -> /api/cron/duty; gebzem-duty-stale-check 06:15 (admin notice in 'live' mode when no real row is on duty); gebzem-poi-sync 01:23 on the 2nd -> /api/cron/poi-sync.
